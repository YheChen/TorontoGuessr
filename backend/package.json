{
  "name": "torontoguessr-backend",
  "private": true,
  "type": "module",
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4.19.2",
    "typescript": "^5",
    "vitest": "^4.1.10"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc --noEmit",
    "generate:verified-locations": "tsx scripts/generate-verified-locations.ts",
    "start": "tsx src/server.ts",
    "test": "vitest run"
  }
}
